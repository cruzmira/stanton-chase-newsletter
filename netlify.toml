[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA fallback (api-proxy routing handled by Functions v2 config.path)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
